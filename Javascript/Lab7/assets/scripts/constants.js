export const GameLevels = Object.freeze({
  easy: "سهل",
  medium: "متوسط",
  hard: "صعب",
});

export const GameState = Object.freeze({
  ready: "ready",
  started: "started",
  finished: "finished",
});

export const WinLoseState = Object.freeze({
  win: "win",
  lose: "lose",
});
